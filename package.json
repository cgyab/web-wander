{
  "name": "webwander",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "wasm": "bash scripts/build-wasm.sh",
    "predev": "npm run wasm",
    "dev": "vite",
    "prebuild": "npm run wasm",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "cargo test",
    "sim": "cargo run --release --bin economy --"
  },
  "devDependencies": {
    "puppeteer-core": "^23.11.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
